-- 함수(집계)

-- COUNT
SELECT COUNT(SAL)
FROM EMP;

--AVG
SELECT AVG(SAL)
FROM EMP;

--SUM
SELECT SUM(SAL)
FROM EMP;

--MIN
SELECT MIN(SAL)
FROM EMP;

--MAX
SELECT MAX(SAL)
FROM EMP;
